# open-videocore — Web UI

Next.js web dashboard for browsing and managing media assets via the open-videocore API.

## Development

```bash
npm install
npm run dev
```

Set `NEXT_PUBLIC_API_URL` in `.env.local` to point at the backend-api instance.

## Status

Skeleton — implementation forthcoming.
